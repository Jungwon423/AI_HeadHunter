/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['lh5.googleusercontent.com', "media.tacdn.com",],
  },
  reactStrictMode: false,
  swcMinify: true,
}

module.exports = nextConfig
